#### Run the following commands to smoothly execute the program

If `UV` is not installed
- pip install uv

Go to your project folder `1_USELLM`
- uv sync --active

To run the project
- uv run main.py

